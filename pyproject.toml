[project]
name = "conecta-4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daniel C", email = "danielcabfe@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "beautifultable>=1.1.0",
    "pyfiglet>=1.0.4",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "basedpyright>=1.39.10",
    "pytest>=9.1.1",
]
